name: build_and_test
on:
  push:
    branches: ['develop']
  pull_request:
    branches: ['develop']
# Warning : schedule is only for default branch
#  schedule:
#    - cron: '0 3 * * 1'
defaults:
  run:
    shell: bash
jobs:

  build:
    runs-on: ubuntu-latest
    container: opencfd/openfoam-dev:2206
    steps:
      - name: install git
        run: |
          sudo apt-get update && sudo apt-get install -y git
      - name: Check out repository code
        uses: actions/checkout@v3
        with:
          submodules: recursive
      - name: Build sedfoam
        run: |
          shopt -s expand_aliases && source /openfoam/bash.rc && 
          export FOAM_USER_LIBBIN=$FOAM_SITE_LIBBIN && ./Allwmake -prefix=group
      - uses: actions/upload-artifact@v2
        with:
          name: sedfoam-binary
          path: /usr/lib/openfoam/openfoam2206/site

  test:
    runs-on: ubuntu-latest
    container: 
      image: opencfd/openfoam-dev:2206
    needs: [build, checkstyle]
    defaults:
      run:
        shell: bash
    steps:
      - name: install git and python3 packages
        run: |
          sudo apt-get update && sudo apt-get install -y git &&
          sudo apt-get -y install python3 python3-pip &&
          sudo pip3 install fluidfoam && mkdir -p /github/home/OpenFOAM/user-v2206/run
      - name: Check out repository code
        uses: actions/checkout@v3
        with:
          submodules: recursive
          path: /github/home/OpenFOAM/user-v2206/run
      - uses: actions/download-artifact@v2
        with:
          name: sedfoam-binary
          path: /usr/lib/openfoam/openfoam2206/site
      - name: chmod sedfoam binary
        run: sudo chmod a+x -R *
        working-directory: /usr/lib/openfoam/openfoam2206/site
      - name: sedfoam test1D sedim
        run: |
          shopt -s expand_aliases && source /openfoam/bash.rc && 
          cd test-ci/1DSedim && cp -f constant/forceProperties.sedim constant/forceProperties &&
          cp -f system/controlDict.sedim system/controlDict  && 
          source /openfoam/bash.rc && chmod -R a+rwX /github/home/OpenFOAM/user-v2206/run &&
          runuser -l sudofoam -c 'source /openfoam/bash.rc && cd /github/home/OpenFOAM/user-v2206/run/sedfoam/test-ci/1DSedim && ./Allrun > /dev/null' &&
          python3 test_Sedimentation.py
        working-directory: /github/home/OpenFOAM/user-v2206/run/sedfoam
      - name: sedfoam test1D bedload
        run: |
          shopt -s expand_aliases && source /openfoam/bash.rc &&
          cd test-ci/1DSedim && cp -f constant/forceProperties.bedload constant/forceProperties &&
          cp -f system/controlDict.bedload system/controlDict &&
          runuser -l sudofoam -c 'source /openfoam/bash.rc && cd /github/home/OpenFOAM/user-v2206/run/sedfoam/test-ci/1DSedim && ./Allrun > /dev/null' &&
          source /openfoam/bash.rc && python3 test_Bedload.py
        working-directory: /github/home/OpenFOAM/user-v2206/run/sedfoam

  advanced-test:
    runs-on: ubuntu-latest
    container: 
      image: opencfd/openfoam-dev:2206
    needs: [build, checkstyle, test]
    defaults:
      run:
        shell: bash
    steps:
      - name: install git and python3 packages
        run: |
          sudo apt-get update && sudo apt-get install -y git &&
          sudo apt-get -y install python3 python3-pip &&
          sudo pip3 install fluidfoam && mkdir -p /github/home/OpenFOAM/user-v2206/run
      - name: Check out repository code
        uses: actions/checkout@v3
        with:
          submodules: recursive
          path: /github/home/OpenFOAM/user-v2206/run
      - uses: actions/download-artifact@v2
        with:
          name: sedfoam-binary
          path: /usr/lib/openfoam/openfoam2206/site
      - name: chmod sedfoam binary
        run: sudo chmod a+x -R *
        working-directory: /usr/lib/openfoam/openfoam2206/site
      - name: sedfoam test1D AvalancheMuI
        run: |
          shopt -s expand_aliases && source /openfoam/bash.rc && 
          source /openfoam/bash.rc && chmod -R a+rwX /github/home/OpenFOAM/user-v2206/run &&
          runuser -l sudofoam -c 'source /openfoam/bash.rc && cd /github/home/OpenFOAM/user-v2206/run/sedfoam/test-ci/1DAvalancheMuI && ./Allrun > /dev/null' &&
          cd test-ci/1DAvalancheMuI && python3 test_1DAvalancheMuI.py
        working-directory: /github/home/OpenFOAM/user-v2206/run/sedfoam

  advanced-test1:
    runs-on: ubuntu-latest
    container: 
      image: opencfd/openfoam-dev:2206
    needs: [build, checkstyle, test]
    defaults:
      run:
        shell: bash
    steps:
      - name: install git and python3 packages
        run: |
          sudo apt-get update && sudo apt-get install -y git &&
          sudo apt-get -y install python3 python3-pip &&
          sudo pip3 install fluidfoam && mkdir -p /github/home/OpenFOAM/user-v2206/run
      - name: Check out repository code
        uses: actions/checkout@v3
        with:
          submodules: recursive
          path: /github/home/OpenFOAM/user-v2206/run
      - uses: actions/download-artifact@v2
        with:
          name: sedfoam-binary
          path: /usr/lib/openfoam/openfoam2206/site
      - name: chmod sedfoam binary
        run: sudo chmod a+x -R *
        working-directory: /usr/lib/openfoam/openfoam2206/site
      - name: sedfoam test 1DBedLoadTurb
        run: |
          shopt -s expand_aliases && source /openfoam/bash.rc && 
          source /openfoam/bash.rc && chmod -R a+rwX /github/home/OpenFOAM/user-v2206/run &&
          runuser -l sudofoam -c 'source /openfoam/bash.rc && cd /github/home/OpenFOAM/user-v2206/run/sedfoam/test-ci/1DBedLoadTurb && ./Allrun > /dev/null' &&
          cd test-ci/1DBedLoadTurb && python3 test_1DBedLoadTurb.py
        working-directory: /github/home/OpenFOAM/user-v2206/run/sedfoam
      - name: sedfoam test 1DBoundaryLayer
        run: |
          shopt -s expand_aliases && source /openfoam/bash.rc && 
          source /openfoam/bash.rc && chmod -R a+rwX /github/home/OpenFOAM/user-v2206/run &&
          runuser -l sudofoam -c 'source /openfoam/bash.rc && cd /github/home/OpenFOAM/user-v2206/run/sedfoam/test-ci/1DBoundaryLayer && ./Allrun > /dev/null' &&
          cd test-ci/1DBoundaryLayer && python3 test_1DBoundaryLayer.py
        working-directory: /github/home/OpenFOAM/user-v2206/run/sedfoam

  advanced-test2:
    runs-on: ubuntu-latest
    container: 
      image: opencfd/openfoam-dev:2206
    needs: [build, checkstyle, test]
    defaults:
      run:
        shell: bash
    steps:
      - name: install git and python3 packages
        run: |
          sudo apt-get update && sudo apt-get install -y git &&
          sudo apt-get -y install python3 python3-pip &&
          sudo pip3 install fluidfoam && mkdir -p /github/home/OpenFOAM/user-v2206/run
      - name: Check out repository code
        uses: actions/checkout@v3
        with:
          submodules: recursive
          path: /github/home/OpenFOAM/user-v2206/run
      - uses: actions/download-artifact@v2
        with:
          name: sedfoam-binary
          path: /usr/lib/openfoam/openfoam2206/site
      - name: chmod sedfoam binary
        run: sudo chmod a+x -R *
        working-directory: /usr/lib/openfoam/openfoam2206/site
      - name: sedfoam test 1DWetAvalanche
        run: |
          shopt -s expand_aliases && source /openfoam/bash.rc && 
          source /openfoam/bash.rc && chmod -R a+rwX /github/home/OpenFOAM/user-v2206/run &&
          runuser -l sudofoam -c 'source /openfoam/bash.rc && cd /github/home/OpenFOAM/user-v2206/run/sedfoam/test-ci/1DWetAvalanche && ./Allrun > /dev/null' &&
          cd test-ci/1DWetAvalanche && python3 test_1DWetAvalanche.py
        working-directory: /github/home/OpenFOAM/user-v2206/run/sedfoam

  checkpep8:
    runs-on: ubuntu-latest
    steps:
      - name: install pycodestyle
        run: |
          sudo pip3 install pycodestyle
      - name: Check out repository code
        uses: actions/checkout@v3
        with:
          submodules: recursive
      - name: Python Style Checker
        run: |
          pycodestyle test-ci  --max-line-length=120 &&
          pycodestyle tutorials  --max-line-length=120

  checkstyle:
    runs-on: ubuntu-latest
    steps:
      - name: install vera++
        run: |
          sudo apt-get install vera++
      - name: Check out repository code
        uses: actions/checkout@v3
        with:
          submodules: recursive
      - name: Check style
        run: |
          shopt -s expand_aliases && ./foamStyleCheck/checkStyle
